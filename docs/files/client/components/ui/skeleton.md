# `client/src/components/ui/skeleton.tsx`
**Purpose:** shadcn/ui skeleton — a pulsing placeholder block.
**Language / Size:** TSX / 291 bytes

## Exports
- `Skeleton({ className, ...props })` — a `<div>` with `animate-pulse rounded-md bg-muted/70`.

## Imports (Internal / External)
- Internal: `cn` (`@/lib/utils`).
- External: react.

## Relationships
- No contexts. The building block composed by `skeletons.tsx` into page/table/card loading states.
