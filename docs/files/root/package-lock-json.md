# `package-lock.json` (root)

Source: `package-lock.json` (390,951 bytes)

## Purpose
npm lockfile pinning the exact resolved versions and integrity hashes for the entire workspace dependency tree (root + `client` + `server` workspaces). Ensures reproducible installs. Auto-generated by npm; not hand-edited.

Per task rules, individual locked packages are **not** enumerated. See the respective `package.json` docs for declared dependency ranges:
- Root: `docs/files/root/package-json.md`
- Client: `docs/files/root/client-package-json.md`
- Server: `docs/files/root/server-package-json.md`
